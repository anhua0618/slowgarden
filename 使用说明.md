# slowgarden 使用说明

一个慢节奏的小院子：用几个文件当存档，你和 AI 一起种地、买东西、做饭。
不刷数据、不拼效率，一天推一格，种下就等它慢慢长。

## 这是什么

slowgarden 不是一个双击就能玩的游戏，它是一套"框架"：

- 规则和内容写在一个 JSON 文件里（有哪些作物、菜谱、商品）
- 一个小引擎负责买、种、收、卖、做饭的逻辑
- 你的进度存在本地一个存档文件里
- 一个命令把当前状态生成成网页，用浏览器就能看

玩法是：你说想干什么，AI 帮你改存档、刷新页面。就像有个人陪你一起经营这个小院子。

## 需要什么

- 一台电脑（Windows / Mac / Linux 都行）
- 装好 Node.js 18 或更高版本（去 nodejs.org 下载安装）
- 会在命令行里敲几条简单命令
- 一个能读写文件的 AI 帮手（比如接了工具的 Claude、GPT 这类）

不需要服务器、不需要数据库、不需要联网。所有东西都在你自己电脑上。

## 怎么开始

把项目下载到本地，进入文件夹，然后：

```bash
node init.js   # 建一个新的存档（第一次玩才需要）
npm start      # 启动本地服务
```

然后用浏览器打开 http://localhost:3000 ，就能在页面上买、种、收、卖、做饭。

服务开着时，页面上每一次操作都会即时写进存档；AI 直接读写同一份 state.json，两边看到的是同一个院子。

想确认一切正常，可以跑 node test.js 自测。

## 怎么玩

slowgarden 的玩法，是你和 AI 一起打理这个院子。你说想做什么，AI 帮你改存档、刷新页面。

常见的几件事：

- 买种子：告诉 AI「买一包豌豆种子」，它会从你的金币里扣钱、把种子放进种子袋。
- 种到地里：「把豌豆种到 1 号地」，种下后要等它长几天。
- 推进时间：「过一天」或「过三天」，作物按天成长。
- 收获：长够天数后「收 1 号地」，作物进仓库。
- 卖钱：「把豌豆卖掉」，换成金币。
- 做饭：材料和厨具齐了，「做一份凉拌番茄」。

每种作物有自己的季节和成长天数，不到季节种不下去，不到天数收不了。这是故意的——它慢，急不来。

## 怎么改成自己的

院子里种什么、卖多少钱、有哪些菜谱，全写在 `catalog.json` 里。想加自己的东西，直接改这个文件：

- 加一种作物：在 `crops` 里照着已有的抄一行，改名字、季节、成长天数、价格。
- 加一道菜谱：在 `recipes` 里写清楚要哪些作物、调料、厨具。
- 换个开局：改 `meta` 里的地块数量、初始金币。

改完让 AI 跑一下 `node engine.js` 检查有没有写错，刷新网页就能看到。

改坏了也不怕，删掉 `state.json` 重新 `node init.js` 就是一个新院子。

详细的字段说明在 README.md 里（那份是给开发者和 AI 看的）。

## 最后

这是一个可以自己长出来的院子。不用追进度，一天种一点，看它慢慢长起来就好。
